# Commit routine Learning Updates to the default branch

Routine Learning Updates will be written as atomic commits directly to the
Learning Vault's default branch rather than creating a branch and pull request
for every learning interaction. The revision check remains mandatory and the
entire update fails if stale; deletion or broad rewriting of learning history,
conflict merges, schema migrations, and Public Export require explicit learner
confirmation instead of using the routine write path.
